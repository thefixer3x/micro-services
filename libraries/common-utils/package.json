{
  "name": "@platform/common-utils",
  "version": "1.0.0",
  "description": "Shared utilities for microservices (logging, errors, validators, HTTP client)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "utilities",
    "logging",
    "errors",
    "validators",
    "microservices"
  ],
  "author": "Platform Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "express-validator": "^7.0.1",
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^20.6.0",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "express": "^4.18.2"
  }
}
