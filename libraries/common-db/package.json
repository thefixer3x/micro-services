{
  "name": "@platform/common-db",
  "version": "1.0.0",
  "description": "Shared database utilities and migration tools for microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["database", "postgresql", "migrations", "microservices"],
  "author": "Platform Team",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "@types/pg": "^8.10.2",
    "typescript": "^5.2.2"
  }
}
